#  maybe create an upper class that does this for us the whole structure is an array of locations

def calcDistances(self, c1, c2):
    """
    Calculate all Distance between cities
    """
    # calcDistanceBtw(c1, c2)
    return


class Location:
    def __init__(self, x, y):
        self.x = x
        self.y = y

    def calcDistanceBtw(self, c1, c2):
        """
        Calculate Distance between two cities
        """
        return
